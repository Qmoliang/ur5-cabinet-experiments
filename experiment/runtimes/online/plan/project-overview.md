# 项目概览

- 任务：澄清 Eq. (17) 参考路径来源，保留现有 UR5e 结果，恢复只给最终目标的 LiuQP 轨迹，并建立球证书 LiuQP 与椭球证书 LiuQP 的公平对照。
- 机器人：MuJoCo Menagerie UR5e，6 自由度。
- 当前场景：`shelf`（FastIRIS/IRIS 论文环境类别的参数化重建）与 `cage`（项目自建扩展），均不是 MIT/IRIS 作者发布的精确场景资产。
- 结论边界：椭球证书用于减少球近似的几何保守性；不预设其能解决需要改变同伦类别的所有局部极小。

## 2026-08-29 增量感知抽屉主任务（已确认）

- 文档类型：中文研究方法与实验协议技术规格，交付 Markdown 与 Word。
- 文档题目：《基于增量深度点云的 UR5 狭窄抽屉进入：LiuQP、IRIS 椭球分离与 VCC/MVT 的统一数学模型及无作弊实验协议》。
- 场景可由研究者规定，路径不可由研究者规定。UR5 从抽屉下方开始，腕部、末端与部分前臂必须真实进入已打开的抽屉。
- 控制器只接收随时间累积的深度点云、机器人状态和同一个最终任务目标；禁止预先完整点云、人工路点、A*/RRT/GCS 路径、IRIS 区域链和随机解锁。
- 主因果对照：同一时刻点云、同一 CenterVox 输出、同一簇中心与数量，球形 LiuQP 因环境球证书连续封闭必经截面而不可达；椭球 LiuQP 仅因各向异性表示恢复通道并由原始控制周期算法到达。
- VCC-inspired CenterVox/MVT/AABB/SIMD 与 IRIS-inspired 椭球分离/支配约束删除只用于实时约束查询和生成，不得通过改变安全裕量、忽略未知区或改变目标来制造成功。
- 数据原则：旧的 `prescribed` 与 `direct` 结果不覆盖；新增结果写入独立目录并记录控制模式、几何证书类型、代码参数和运行时间。
