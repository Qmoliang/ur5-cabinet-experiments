# 第二章双重审查记录：椭球型 LiuQP 数学推导

## 规格符合性审查

本章以 `LiuQP_Mathematical_Derivation_and_Theory_Guide_English_Expanded.docx` 为母版。源文档经 Word 只读解析得到 691 个段落、407 个公式对象、1 张表和 1 个节，并用其生成源码 `tools/build_liuqp_theory_guide.py` 逐式核对公式编号与原始文本。

用户要求的继承边界已经落实。公式（1）—（20）的图运动学、模块点速度、末端反馈、任务堆叠和关节界沿用母版；工作空间公式（21）—（22）沿用原意并补明控制周期量纲。从障碍几何开始，补充式 A—H、公式（23）、（24）、（26）、（27）均改写为椭球支持几何。公式（25）的软任务目标保持不变。

本章没有引入 RRT、人工路点、完整参考路径、随机扰动、`ellipsoid-fast`、IRIS 区域膨胀或 GCS。IRIS-inspired 仅指真实最近法向、障碍支持点和分离平面的凸几何构造。VCC-inspired MVT、CenterVox、AABB 与 SIMD 被明确留到后续查询加速章节，不混入本章距离公式。

符号方向已经统一为从机器人指向障碍物的 \(\tilde s\)，并写明它与第一章障碍物指向机器人的 \(n\) 满足 \(n=-\tilde s\)。全部速度行、CONTACT 撤离方向和间隙导数均按这一约定核对。

## 数学与质量审查

球—单椭球分支从带等式约束的最近点问题出发，推导出唯一标量根 \(F(\lambda)=0\)、一阶导数、受保护 Newton、必要二分和乘子热启动。一般机器人形状—障碍形状—方向不确定性分支从支持和目标 \(\phi(s)\) 出发，推导欧氏梯度、Riemannian 梯度、切空间 Hessian、受保护 Newton、梯度回退、Armijo 回溯和法向热启动。文档明确说明这两个求解分支不会对同一纯球—椭球 pair 重复执行。

分离平面之后重新得到原始间隙 \(c\)、硬安全间隙 \(h=c-d_{\mathrm{safe}}\)、速度级硬约束（23E）、椭球支持最小值冗余判据以及 NORMAL、NEAR、CONTACT/RECOVERY 三状态。安全余量没有取消，也没有重复并入核心椭球、方向不确定性与覆盖偏置。

独立数值特例的最大误差均低于 \(1.1\times10^{-9}\)：球极限误差 \(8.363\times10^{-17}\)，椭球表面方程误差 \(2.220\times10^{-16}\)，法向对齐误差 \(5.551\times10^{-17}\)，支持点恒等式误差 \(6.939\times10^{-18}\)，标量根残差 \(1.110\times10^{-16}\)，间隙导数有限差分误差 \(1.091\times10^{-9}\)。

实现回归测试结果为 `test_ellipsoid_reproduction.py` 9 项全部通过，`test_native_ellipsoid_support.py` 13 项全部通过。写作检查器未发现多余粗体、正文列表、机械衔接词、主观第一人称或写作过程泄漏；其连续非空行提示全部来自多行公式和 Markdown 表格，人工复查后无需改写。

最终 Markdown 含 16,529 个字符、13 个二级节、73 对显示公式定界符和 73 个互不重复的公式标签；占位符、错误双撇号、RRT 与 `ellipsoid-fast` 的计数均为零。SHA-256 为 `6FA99712196D6AB6803596796DE3A217BB95CCEEE604381B026AC25C0358706C`。

## 保留限制

外点标量乘子推导不覆盖机器人球心真正进入障碍核心椭球的情形；当前代码中的内部点回退只能作为恢复保护，不能称为精确有符号距离。一般支持和的原生正式核实现受保护 Riemannian Newton；Python 参考实现的用途和正式性能结论需要在代码审核章单列。源 DOCX 的内容与公式已从 Word 公式对象及精确生成源码核对，但本机缺少 LibreOffice，因此没有完成源 DOCX 的逐页渲染视觉审查。

审查结论：第二章已达到交付用户逐段审阅的条件，但在用户确认继承边界和法向约定前不标记为全文冻结。
