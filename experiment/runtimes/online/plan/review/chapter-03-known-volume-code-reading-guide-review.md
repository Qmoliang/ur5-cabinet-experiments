# 第三章环境已知体积实验代码阅读指南审查

## 阶段 1：规格合规检查

- 状态：通过。
- 文件：`chapters/03_环境已知体积实验代码阅读指南.md`。
- 规模：26,927 个字符，736 行，13 个二级标题，30 个三级标题。
- 结构：包含 2 个 Mermaid 图、主调用链、静态初始化、完整控制周期、融合 C++ 内核展开、逐文件职责、公式—代码双向索引、变量词典、结果链、六轮阅读顺序和断点观察表。
- 数学对应：共扫描到 78 处第二章公式引用；覆盖运动学、任务反馈、关节/工作空间界、椭球最近点、切平面、ordered erase-remove、三状态 QP 和 Euler 更新。
- 范围控制：明确当前实验为已知环境、\(U=0,\delta=0\)；区分默认 C++ 融合热路径、Python 参考/回退路径和未启用的一般支持和分支。
- 关键语义：明确 MVT 只返回候选；CONTACT 参数只维护恢复 pair 的优先级；MuJoCo contact 只读审计；唯一最终目标不构成路径。
- Markdown：代码围栏 6 个且成对，Mermaid 围栏 2 个。

## 阶段 2：质量检查

- 状态：通过。
- 逻辑：正文按照“实验输入 → 静态初始化 → 单周期 → native 内核 → QP → 状态更新 → 日志验证”形成连续输入到输出流程，没有按文件平铺替代方法流程。
- 语言：风格脚本的禁用中英文过渡词、主观表达、过程指令泄漏和占位符检查均通过。
- 列表使用：脚本报告的列表位于文件关系、函数名拆解、代码定位规则和调试步骤，属于用户明确要求的阅读指南索引；核心方法仍用连续段落解释。
- 双下划线报告：脚本将 Python 标识符 `__init__` 识别为粗体标记，属于代码标识符误报。
- 事实核验：关键函数和调用点已从当前源文件重新定位；`tests/check_experiment.py` 通过，重新确认 368 个共享完整体积单元、椭球成功、球受阻、两组零穿透，以及球同 QP 的鲁棒求解器验证仍受阻。
- SHA-256：`08850FAA96E893582A6257F0BF9880E0B02C5D3DCD83549DEDCDEC74E22A116C`。

## 剩余边界

`collision.cpp` 是 DLL 源码，而 Python 实际执行 `collision.dll`；本轮通过函数接口和运行测试核对行为，没有重新编译 DLL 做逐二进制来源证明。完整体积审计覆盖 `scene.json` 盒体，MuJoCo 真值接触来自 `drawer.xml`；本章已将两份场景资产的一致性列为独立审计契约。该指南限定于 `LiuQP_known_volume`，不覆盖增量深度点云正式 v5 的感知与在线地图链。



