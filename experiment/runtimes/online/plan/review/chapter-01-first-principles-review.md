# 第一章规格与质量审查

审查日期：2026-09-01  
审查对象：`chapters/01_第一性原理与问题定义.md`

## 规格符合性审查

- 通过。文件存在，正文 6137 个字符，超过任务单的 3000 字符下限。
- 通过。包含 1.1—1.8 共 8 个二级小节，19 组陈列公式，公式编号连续为 1—19，开闭定界符各 19 个。
- 通过。本章没有报告 v4.3/v4.4 胜负，没有把局部停滞写成全局闭塞，也没有把 IRIS、RRT、A* 或 GCS 写成控制器组成。
- 通过。球和椭球允许得到不同代理数量；公平性定义为共同输入证书、覆盖语义、尺度规则和安全定义，而非强制数量相等。
- 通过。NEAR 的软项被准确写为 pair 法向关节速度分量的二次惩罚，硬碰撞行在三个状态中均保留。

## 数学与实现一致性审查

- 法向约定在 1.4 节显式固定为“障碍物指向机器人”，并在 1.5 节说明代码 `normal_to_obstacle` 使用相反方向。
- 代码默认值核对为 `d_safe=0.006 m`、`d_near=0.040 m`、`d_contact=0`，与正文 6 mm、40 mm 和零接触阈值一致。
- QP 硬行核对为 `normal_to_obstacle @ J_i @ qdot <= h_i / dt`，与正文式（18）一致。
- 数值特例检查令 `Q=R^2I`、`U=0`、`delta=0`，式（12）的椭球间隙与球间隙误差为 `2.77555756156289e-17`，通过 `1e-12` 门槛。

## 质量审查

- 通过。论证顺序从物理任务到几何证书，再到精确间隙、QP 和查询加速，没有用实验版本流水账替代原理。
- 通过。正文没有 TODO、待补、占位符、冲突标记、制表符或行尾空白。
- 通过。机械式连接词“首先、其次、最后、此外、另外、接下来、总之”计数为 0。
- 通过。明确区分了局部受阻、QP 不可行和连续代理闭塞，也限定椭球只解决各向同性外包造成的部分保守性。

## 尚未纳入本章的内容

本章不承担原始论文逐式引用核验、CenterVox 覆盖半径推导、受保护 Newton/二分算法细节、MVT/SIMD 数据结构、v4.3/v4.4 实验统计或代码逐行讲解。这些内容应在用户确认本章表述后按单独任务单展开，避免一次写作跨越多个审查门。
