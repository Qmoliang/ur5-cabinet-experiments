# 结果表数据契约

| 表 | 用途 | 行 | 指标 | 数据源 | 更新方式 |
|---|---|---|---|---|---|
| `results.csv` | 主球/椭球对照 | sphere、ellipsoid | 数量、覆盖、体积比、误差、安全性、控制耗时、候选对 | 两组 `summary.json` 与 `coverage.json` | `aggregate.py` 从真实日志生成 |

本次是确定性机制试验，每种表示先运行一次。计时只报告本机单次分位数，不报告均值±标准差，也不作显著性结论。
